#include<stdio.h> 

int main() {
    // int a[]={34, 232, 23};
    // printf("the value of a[0] is %d\n", a[0]);
    // printf("the value of a[0] is %d\n", a[1]);
    // printf("the value of a[0] is %d\n", a[2]);

    float a[]={3.4, 2.32, 2.3};
    printf("the value of a[0] is %f\n", a[0]);
    printf("the value of a[0] is %f\n", a[1]);
    printf("the value of a[0] is %f\n", a[2]);

    return 0;
}